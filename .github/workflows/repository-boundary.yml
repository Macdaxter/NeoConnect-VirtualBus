name: Public repository boundary

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  boundary:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject internal or binary release material
        shell: pwsh
        run: ./tools/Test-RepositoryBoundary.ps1
